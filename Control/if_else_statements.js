const personAge = 20;

// if
if (personAge >= 18) {
  console.log("Eligible to vote");
}

const weather = "hot";

// if...else
if (weather === "hot") {
  console.log("The weather is hot outside!");
} else {
  console.log("The weather is not hot outside");
}
