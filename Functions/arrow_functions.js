// a simplified way of writing functions

const add = (a, b) => a + b;
console.log(add(1, 2));

const hello = () => "Hello";
console.log(hello());

const subtract = (a, b) => {
  const result = a - b;
  return result;
};
console.log(subtract(10, 7));
